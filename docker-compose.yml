
services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5433:5432"
    networks:
      - sqltrain
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./SQLTrainer_backend
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - sqltrain

  frontend:
    build: ./sqltrainer_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - sqltrain
    volumes:
      - ./sqltrainer_frontend/src:/app/src

networks:
  sqltrain:

